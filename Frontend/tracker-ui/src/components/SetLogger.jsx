export const SetLogger = ({ exerciseId }) => {
    return (
        <div className="set-logger">
            <h4>Log Sets</h4>
            {/* Input fields for reps and weight will go here */}
        </div>
    );
};
